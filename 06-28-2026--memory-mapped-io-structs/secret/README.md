# secret/ — DO NOT READ

This folder is for **grading only**. It contains the simulation harness, the
reference solution, and the rubric.

If you are the learner: reading anything in here voids the exercise. You only
ever interact with the harness through the commands in `BUILD.md`.

Contents:
- `harness/` — the QTimer-16 silicon model and the self-test main.
- `reference/` — a corrected `include/` + `src/` that passes the rubric.
- `rubric.md` — pass/fail checklist for the grader.
- `solution.md` — root cause + reference patch (revealed only on strike 3).
- `.strikes` — grading attempt counter (created on first grade).
